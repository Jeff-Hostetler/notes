{{outlet}}

<div class="textBox">
  {{textarea placeholder="Add a note!" value=noteCopy}}
</div>
<div>
  {{textarea value=noteCopy class="exampleTextArea"}}
</div>
<button {{action 'newNote'}}>Save Note</button>

{{#each note in model}}
<div class="note">
  {{note.body}}
</div>
{{/each}}
